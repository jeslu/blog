<p id="notice"><%= notice %></p>

<h1>Cuenta Clientes</h1>

<table>
  <thead>
    <tr>
      <th>Cliente</th>
      <th>Activo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cuenta_clientes.each do |cuenta_cliente| %>
      <tr>
        <td><%= cuenta_cliente.cliente_id %></td>
        <td><%= cuenta_cliente.activo %></td>
        <td><%= link_to 'Show', cuenta_cliente %></td>
        <td><%= link_to 'Edit', edit_cuenta_cliente_path(cuenta_cliente) %></td>
        <td><%= link_to 'Destroy', cuenta_cliente, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cuenta Cliente', new_cuenta_cliente_path %>
