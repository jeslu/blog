<%= form_with(model: cuenta_cotizacion, local: true) do |form| %>
  <% if cuenta_cotizacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cuenta_cotizacion.errors.count, "error") %> prohibited this cuenta_cotizacion from being saved:</h2>

      <ul>
        <% cuenta_cotizacion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cuenta_id %>
  
    <%= form.collection_select(:cuenta_id, Cuenta::CuentaCliente.all, :id, :id, options = {}, {:class => "ui search dropdown"}) %>
  </div>

  <div class="field">
    <%= form.label :producto_id %>
    
    <%= form.collection_select(:producto_id, Producto::Producto.all, :id, :nombre, options = {}, {:class => "ui search dropdown"}) %>
  </div>

  <div class="field">
    <%= form.label :cantidad %>
    <%= form.number_field :cantidad %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
